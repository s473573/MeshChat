#:kivy 1.0

<JoinScreen>:
    BoxLayout:
        id: known_networks

        orientation: 'vertical'
        spacing: dp(20)
        padding: dp(50)
        # input box to join a new one
        # Label:
        #     text: "many known networks here"
        # list of networks